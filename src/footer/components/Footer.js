import React,{Component} from 'react'

    const Footer = props =>{
            let {footer} = props.data

            return(
                <h1>{footer}</h1>
            )
    }

    export default Footer